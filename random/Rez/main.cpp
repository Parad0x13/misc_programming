#include <iostream>
#include <vector>
#include "PTron.h"
#include "PTron_Examples.h"

int main() {
	PTronXOR(0, 0);
	/*PTron *ptron_1 = new PTron(1);
	PTron *ptron_2 = new PTron(50);
	PTron *ptron_3 = new PTron(-12);

	ptron_1->addInput(ptron_2);
	ptron_1->addInput(ptron_3);
	ptron_1->log();

	std::vector<double>trainingInputStrengths;
	trainingInputStrengths.push_back(50.0f);
	trainingInputStrengths.push_back(-12.0f);

	for(int d=0;d<2;d++) {
		ptron_1->train(trainingInputStrengths, 1.0f);
		ptron_1->log();
	}*/
}
